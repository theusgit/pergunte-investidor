<%- include ('partials/header.ejs') %>

<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container">
        <h1>Faça sua pergunta</h1>
        <hr>
        <a href="/perguntar" class="btn btn-primary">Perguntar</a><hr>
        <% perguntas.forEach(pergunta =>{ %>
            <div class="card">
                <div class="card-body">
                    <h3><%=  pergunta.titulo %></h3><hr>
                    <h6><%= pergunta.descricao %></h6>
                </div>
                <div class=card-footer>
                    <a href="/pergunta/<%= pergunta.id %>"><button class="btn btn-primary">Responder</button></a>
                    <a href="/pergunta-delete/<%= pergunta.id %>"><button class="btn btn-danger">excluir</button></a>
                </div>
            </div><br>
        <% }) %>

    </div>
    
</body>
<%- include ('partials/footer.ejs') %>